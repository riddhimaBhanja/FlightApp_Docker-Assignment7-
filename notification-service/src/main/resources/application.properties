spring.application.name=notification-service
server.port=8083

# Eureka Client
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
eureka.instance.prefer-ip-address=true

# MongoDB
spring.data.mongodb.uri=mongodb://root:root@mongodb:27017/notification_db?authSource=admin
spring.data.mongodb.auto-index-creation=true

# SMTP Configuration - Gmail (ACTIVE)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:riddhimabhanja2003@gmail.com}
spring.mail.password=${MAIL_PASSWORD:lmvyuduaudbsjtxi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# For testing with MailHog (disabled):
#spring.mail.host=${MAIL_HOST:mailhog}
#spring.mail.port=${MAIL_PORT:1025}
#spring.mail.username=${MAIL_USERNAME:}
#spring.mail.password=${MAIL_PASSWORD:}
#spring.mail.properties.mail.smtp.auth=false
#spring.mail.properties.mail.smtp.starttls.enable=false
#spring.mail.properties.mail.smtp.starttls.required=false

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Logging
logging.level.com.flightapp.notification=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
